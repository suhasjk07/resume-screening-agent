# Optional. Only needed if you want LLM-generated recruiter-style rationales
# instead of the built-in rule-based rationale. The agent runs perfectly
# fine without this key.
#
# Get a key at https://console.anthropic.com/
# Then either export it in your shell, or `pip install python-dotenv` and
# load this file, or just:
#   export ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=
